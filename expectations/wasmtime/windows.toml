version = 1

[[suite]]
name = "WASI Rust tests [wasm32-wasip1]"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "dangling_fd"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "dangling_symlink"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "fd_fdstat_set_rights"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "fd_readdir"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "file_allocate"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "file_pread_pwrite"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "file_seek_tell"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "file_truncation"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "file_unbuffered_write"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "interesting_paths"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "isatty"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "nofollow_errors"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "overwrite_preopen"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_exists"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_filestat"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_link"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_open_create_existing"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_open_dirfd_not_dir"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_open_missing"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_open_read_write"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_rename"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_rename_dir_trailing_slashes"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "path_symlink_trailing_slashes"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "readlink"
action = "skip"

# Wasmtime returns ERRNO_INVAL for path_remove_directory with a trailing slash
[[suite.test]]
name = "remove_directory_trailing_slashes"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "remove_nonempty_directory"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "renumber"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "stdio"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "symlink_create"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "symlink_filestat"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "symlink_loop"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "truncation_rights"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 1 test
[[suite.test]]
name = "unlink_file_trailing_slashes"
action = "skip"

[[suite]]
name = "WASI Rust tests [wasm32-wasip3]"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-flags-and-type"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-hard-links"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-io"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-metadata-hash"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-mkdir-rmdir"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-open-errors"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-read-directory"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-rename"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-set-size"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-stat"
action = "skip"

# Wasmtime Windows filesystem semantics differ for this Preview 3 test
[[suite.test]]
name = "filesystem-unlink-errors"
action = "skip"

# requires a UDP peer managed outside the component under test
[[suite.test]]
name = "sockets-udp-send"
action = "skip"
